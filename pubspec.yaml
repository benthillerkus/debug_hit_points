name: debug_hit_points
description: "Visually debug where a Widget can be tapped"
version: 1.0.0
homepage: https://gihub.com/benthillerkus/

environment:
  sdk: ^3.0.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
